# config path
    ~/.bashrc
> execute at any (virtual)terminal startup
```
~/.bash_profile
```
> only exec at login
# environment variables
* mostly define in ~/.bash_profile
```bash
export $VAR=$value
```
